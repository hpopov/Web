To use this project you have to install gulp:
npm install gulp --save-dev
Prerequisities:
You have to has node.js with npm installed (go to official site)
Also you have to install gulp globally (npm install --global gulp)